# Rust
/target
Cargo.lock.bak

# Node
node_modules/
dist/
dist-ssr/
*.local
*.tsbuildinfo

# Tauri
src-tauri/target/
src-tauri/gen/

# Website build output
website/dist/
website/node_modules/
website/.astro/

# Playwright
test-results/
playwright-report/
blob-report/

# Local tooling state
.impeccable/

# Editors / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/

# Local data, never committed
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm
.env
.env.local

# Demo-mode fixture (src/demo/mockTauriCore.ts is real source and stays
# tracked): this is a real machine-scan export, hand-sanitized once for
# local screenshotting, not yet reviewed for whether it should ever live
# in git history. Ignored defensively so a broad `git add -A` elsewhere
# in this checkout can't sweep it into a commit unreviewed.
src/demo/fixture.json

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
